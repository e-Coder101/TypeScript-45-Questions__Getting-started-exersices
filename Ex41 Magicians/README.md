# Solution 41 of 45

**Task:** Make a array of magician’s names. Pass the array to a function called show_magicians(), which prints the name of each magician in the array.

**Solution:**
- Create an array *'magicians'*.
- Create a funtion that takes the array as an argument and and prints all the items in the array.
- Call the function and the array *'magicians'* to it.